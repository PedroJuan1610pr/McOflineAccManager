// Web panel configuration (manage the bots from other devices on your
// local network, e.g. http://192.168.1.X:3000).
//
// ⚠️ IMPORTANT: whoever has this password can read your accounts' chat
// and send them commands. Change it before using this, and don't expose
// this port outside your local network (don't set up port-forwarding on
// your router).
module.exports = {
  port: 3000,
  password: 'change-this',
}
